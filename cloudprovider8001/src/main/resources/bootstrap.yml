spring:
  application:
    name: cloud-payemt-service
  cloud:
    nacos:
      config:
        file-extension: yaml
        namespace: huangwei-dev
        ##是否启用nacos自动配置
        enabled: true
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: huangwei-dev
  profiles:
    active: deveop
  rabbitmq:
    host: 101.133.153.60
    port: 5672
    username: rabbitmq
    password: rabbitmq
    virtual-host: guest
